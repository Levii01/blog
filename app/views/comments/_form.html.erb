<%= simple_form_for ([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.input :commenter %>
  </p>

  <p>
    <%= f.input :body %>
  </p>

  <p>
    <%= f.button :submit %>
  </p>
<% end %>
